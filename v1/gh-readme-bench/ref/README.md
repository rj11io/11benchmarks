# Frozen reference PDFs

These files are the only shared biographical inputs:

- `RJ_CV.pdf` — concise two-page A4 CV.
- `RJ_CV_max.pdf` — extended six-page A4 CV.

Do not add a shared transcript. Each run must independently extract, reconcile,
and edit the content. The pinned hashes live in
`benchmark/reference-manifest.json`; changing a PDF creates a new cohort.
